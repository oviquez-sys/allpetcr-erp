========================================================
   ALLPETCR ERP — GUÍA PARA SUBIRLO A LA NUBE (SERVIDOR)
========================================================

Esta guía es para cuando pases el sistema de tu computadora a un servidor en
internet (un VPS: DigitalOcean, Hetzner, AWS Lightsail, etc.), para que funcione
siempre y desde cualquier lado. Aclaración: NO se corre "sobre OneDrive" —
OneDrive es solo para guardar archivos. El sistema corre en un servidor.

Todas las mejoras de seguridad YA están en el código. Se activan poniendo las
variables de entorno correctas (ver el archivo .env.example). No hay que
reprogramar nada.


-------------------------------------------------
0) LO ÚNICO URGENTE DE HOY (antes de la nube)
-------------------------------------------------
Mientras probás en tu computadora, la base de datos (db.sqlite3) está dentro de
OneDrive. Esa combinación (SQLite + OneDrive) puede CORROMPER la base: OneDrive
sube el archivo a medio escribir o lo bloquea al sincronizar.

Recomendación inmediata (5 minutos), aunque todavía no vayas a la nube:
  1. Creá una carpeta FUERA de OneDrive, por ejemplo:  C:\allpetcr-datos\
  2. Apagá el sistema (Ctrl+C en la ventana negra).
  3. Mové el archivo db.sqlite3 a esa carpeta nueva.
  4. Antes de encender, definí la variable (una sola vez, en esa ventana):
        set DJANGO_DB_PATH=C:\allpetcr-datos\db.sqlite3
     (o dejala fija en un .env / en las variables del sistema de Windows).
  5. Encendé normal. Los RESPALDOS sí pueden seguir yendo a OneDrive: los .zip
     son seguros de sincronizar; el que no debe estar ahí es la base viva.


-------------------------------------------------
1) PREPARAR EL SERVIDOR (una vez)
-------------------------------------------------
- Un VPS con Ubuntu.
- Instalar: Python 3.12, PostgreSQL, nginx, y un certificado HTTPS gratis
  (Let's Encrypt / certbot).
- Crear la base en PostgreSQL (usuario y clave fuerte).


-------------------------------------------------
2) VARIABLES DE ENTORNO (el interruptor de seguridad)
-------------------------------------------------
Copiá .env.example como .env y completá los valores reales. Lo clave:

  DJANGO_PRODUCTION=1          -> activa TODO el blindaje y apaga DEBUG
  DJANGO_SECRET_KEY=...        -> llave larga y aleatoria (obligatoria)
  DJANGO_ALLOWED_HOSTS=tudominio.com
  DJANGO_CSRF_ORIGINS=https://tudominio.com
  POSTGRES_HOST=... (y las demás POSTGRES_*)
  DJANGO_ENABLE_AXES=1         -> bloqueo de login por fuerza bruta

Generar la llave secreta:
  python -c "import secrets; print(secrets.token_urlsafe(64))"

Con DJANGO_PRODUCTION=1, el sistema por sí solo:
  - Apaga DEBUG (no muestra errores internos a nadie).
  - Obliga HTTPS y cookies seguras (sesión y CSRF cifradas).
  - Activa HSTS.
  - Escribe logs a archivo con rotación (carpeta logs/ o DJANGO_LOG_DIR).
  - Si olvidás la llave secreta, NO ARRANCA (a propósito, para protegerte).


-------------------------------------------------
3) INSTALAR Y ARRANCAR
-------------------------------------------------
  pip install -r requirements.txt
  python manage.py migrate
  python manage.py collectstatic --noinput
  python manage.py createsuperuser        (tu cuenta de dueño)

Servir con gunicorn detrás de nginx (nginx hace el HTTPS y sirve /static y /media).


-------------------------------------------------
4) USUARIOS Y PERMISOS (mínimo privilegio)
-------------------------------------------------
IMPORTANTE cambio de seguridad: ahora SOLO el dueño es superusuario.

  - Tu cuenta (dueño), acceso total:
        python manage.py asignar_rol TU_USUARIO Gerente --dueno

  - Otros gerentes (pueden gestionar productos/clientes/proveedores, pero NO
    borrar la base ni tocar usuarios ni configuración):
        python manage.py asignar_rol juan Gerente

  - Cajero (solo POS y su caja; ya no puede ni asomarse al panel /admin):
        python manage.py asignar_rol maria Cajero --crear --password "ClaveFuerte#2026"

Al crear usuarios ahora es OBLIGATORIO poner una contraseña fuerte (se acabó la
clave por defecto). Sumá doble factor (2FA) para las cuentas con más poder cuando
puedas: es la siguiente capa recomendada.

-------------------------------------------------
4b) ENDURECER LA BITÁCORA (fraude interno, FRA-002)
-------------------------------------------------
Un paso más de la Fase 1: que editar o borrar la bitácora (AuditLog) por
SQL directo o "manage.py shell" deje de ser gratis. Se corre UNA VEZ, sobre
la base de PostgreSQL de producción, después de migrate:
        python manage.py endurecer_auditlog              # solo muestra el estado
        python manage.py endurecer_auditlog --confirmar  # aplica el REVOKE

Límite conocido (lo explica el propio comando al correrlo): el rol de la
app es dueño de la tabla, así que sube el costo de manipularla, pero no lo
vuelve imposible — un dueño siempre puede volver a otorgarse el permiso.
Blindaje completo requeriría un rol de base de datos separado, más
trabajo, fuera de alcance de este paso.


-------------------------------------------------
5) RESPALDOS EN LA NUBE (regla 3-2-1)
-------------------------------------------------
El sistema ya hace respaldos con rotación. En el servidor:
  - Agendá el respaldo diario (cron).
  - Guardá una copia en un lugar INDEPENDIENTE del servidor (otro bucket/nube,
    o descargarlos periódicamente). Si el servidor se pierde, los respaldos no
    deben perderse con él. Ver 5c) para la copia automática e inmutable.
  - Conservá aparte el respaldo del cierre de cada mes.


-------------------------------------------------
5b) REPORTE DIARIO A LOS SOCIOS (fraude interno, FRA-004)
-------------------------------------------------
Control detectivo: un correo automático cada día con ventas, regalías/
descuentos que necesitaron autorización de gerente, arqueo de caja, y
cualquier edición/borrado sobre documentos financieros. Reutiliza el mismo
SMTP de las facturas — no requiere nada nuevo salvo agendarlo.

Configurá REPORTE_DIARIO_DESTINATARIOS en el .env (correos de ambos socios,
separados por coma) y agendá, una vez al día (de madrugada, resume el día
anterior):
        python manage.py reporte_diario

En Windows, Programador de tareas → Crear tarea básica → Diariamente →
Acción "Iniciar un programa" → apuntá al mismo Python/entorno virtual del
proyecto con el argumento "manage.py reporte_diario". Mismo mecanismo que
se recomienda para "manage.py reconciliar" (ver también RESPALDOS.txt).

Probar antes de agendar en firme:
        python manage.py reporte_diario --fecha AAAA-MM-DD --destinatarios tucorreo@ejemplo.com


-------------------------------------------------
5c) RESPALDO INMUTABLE A BACKBLAZE B2 (fraude interno, FRA-005)
-------------------------------------------------
Con el servidor en DigitalOcean administrado por los dos socios, mandarle
una copia del respaldo al "otro socio" no alcanza: los dos ya tienen el
mismo nivel de acceso al servidor. Hace falta un tercero que NINGUNO de
los dos pueda alterar desde ahí — Object Lock de Backblaze B2 en modo
Compliance hace exactamente eso: una vez subido un archivo, nadie puede
borrarlo ni modificarlo antes de que venza la retención, ni siquiera el
dueño de la cuenta.

Se evaluó DigitalOcean Spaces primero y se descartó: no tiene Object Lock
(solo reglas de expiración, que BORRAN, no protegen) y sus llaves de
acceso no tienen un nivel "solo escritura, sin borrado". Backblaze B2 sí
tiene ambas piezas.

PASO A PASO (una sola vez, por un socio, desde el panel web de B2):

1. Crear el bucket:
   - En el panel de B2, "Create a Bucket".
   - Nombre: por ejemplo "allpetcr-respaldos".
   - "Object Lock": ACTIVAR al crear el bucket (más simple que activarlo
     después, aunque también se puede sobre uno ya existente).

2. Configurar la retención por defecto del bucket:
   - Dentro del bucket, configuración de Object Lock → "Default Retention".
   - Modo: COMPLIANCE (no Governance — Compliance es el que nadie, ni el
     dueño de la cuenta, puede saltarse antes de tiempo).
   - Período: 30 días.
   - A partir de acá, TODO archivo que se suba a este bucket queda
     protegido automáticamente — no hace falta que la app pida nada
     especial al subir.

3. Crear la Application Key para el servidor:
   - Panel de B2 → "Application Keys" → "Add a New Application Key".
   - "Allow access to Bucket(s)": elegir SOLO "allpetcr-respaldos" (nunca
     "All").
   - "Type of Access": **Write Only**. NO elegir "Read and Write" — esa
     llave, aunque el bucket tenga Object Lock, podría listar y ver los
     respaldos; "Write Only" no puede ni listar ni borrar.
   - "File name prefix": opcional, restringe la llave a nombres que
     empiecen con "respaldo_allpetcr_" (mismo prefijo que ya usa el
     comando) — un paso extra de acotamiento, no imprescindible.
   - "Allow List All Bucket Names": dejar SIN marcar.
   - Guardar el "keyID" y la "applicationKey" que se muestran UNA sola vez
     — no se pueden volver a ver después.

4. Completar el .env del servidor (ver .env.example):
        B2_BUCKET=allpetcr-respaldos
        B2_KEY_ID=<el keyID del paso 3>
        B2_APPLICATION_KEY=<el applicationKey del paso 3>
        B2_ENDPOINT=<el endpoint S3 de tu región, ej. s3.us-west-004.backblazeb2.com>

5. Probar: correr "python manage.py respaldar" y confirmar en el panel de
   B2 que el archivo llegó, y que su "Retention" muestra "Compliance,
   expira en 30 días".

Con esto configurado, cada corrida de "python manage.py respaldar" sube
automáticamente el zip a B2 además de guardarlo local — no hace falta
ningún paso manual día a día.

LÍMITE CONOCIDO (documentado a propósito): esto protege la copia YA
subida, no la máquina que la sube. Si alguien roba las credenciales del
servidor (.env), puede seguir subiendo respaldos nuevos (es justo lo que
debe poder hacer), pero no puede borrar ni sobrescribir los que ya están
protegidos por Object Lock. Purgar un archivo bloqueado exige, aunque sea
con la cuenta completa de B2, esperar a que venza el plazo de 30 días —
no hay atajo.

RESTAURAR DESDE B2 (manual, si hace falta): descargar el zip desde el
panel web de B2, o con el cliente S3 de AWS apuntando al endpoint de B2:
        aws s3 cp s3://allpetcr-respaldos/NOMBRE.zip . --endpoint-url https://TU_ENDPOINT
y después seguir el flujo normal de "python manage.py restaurar --archivo
NOMBRE.zip --confirmar" con ese zip ya en la carpeta local de respaldos.


-------------------------------------------------
6) VERIFICACIÓN FINAL DE SEGURIDAD
-------------------------------------------------
Con las variables de producción puestas, corré:
        python manage.py check --deploy
Debe salir SIN advertencias (o solo las que decidas aceptar conscientemente).


-------------------------------------------------
RESUMEN DE VARIABLES  ->  ver el archivo .env.example
========================================================
