# Versión probada y verificada (150 pruebas en verde). Django 6.0 aún no está
# validado; use 5.2 para garantizar el comportamiento probado.
Django>=5.2,<5.3
openpyxl>=3.1
python-barcode>=0.15   # etiquetas con código de barras (Code128)
psycopg2-binary>=2.9   # PostgreSQL en producción (VPS)
gunicorn>=21.0         # servidor de aplicación en producción
django-axes>=6.0       # bloqueo de login por fuerza bruta (activar con DJANGO_ENABLE_AXES=1)
anthropic>=0.120       # asistente de chat del ERP (core/views.py lo importa al arrancar)
boto3>=1.34            # respaldo a Backblaze B2 (API S3-compatible), FRA-005
django-storages[s3]>=1.14  # fotos de producto en S3/R2/B2 (MEDIA_STORAGE_BACKEND=s3), Bloque 1
djangorestframework>=3.15  # API del catálogo y pedidos para el sitio web, Bloque 3
django-cors-headers>=4.4   # CORS de la API por variable de entorno, Bloque 3
truststore>=0.9        # TLS contra el almacen de Windows: sin esto, un antivirus con inspeccion SSL rompe el envio de correos
playwright>=1.47      # Chromium para imprimir el recibo a PDF con el diseno intacto (core/pdf.py)
pywin32>=306; sys_platform == "win32"   # imprimir en las impresoras de Windows (tiquete y
                       # etiquetas). El marcador sys_platform es obligatorio: pywin32 NO existe
                       # para Linux y sin él "pip install -r requirements.txt" falla en el
                       # servidor. impresion/windows.py ya lo importa solo al momento de
                       # imprimir, así que el ERP levanta igual en un servidor sin impresora.
Pillow>=10.0          # dibuja la etiqueta de 35x25 mm que se manda a la Xprinter
